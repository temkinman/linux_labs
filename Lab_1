#!/bin/bash

# Написать скрипт поиска  файлов заданного размера в заданном каталоге
# (имя каталога задаётся пользователем  в качестве третьего аргумента 
# командной строки). Диапазон (мин.- мах.) размеров файлов задаётся пользователем
# в качестве первого и второго аргумента  командной строки. На консоль выводится
# полный путь к файлу и его размер.  Проверить работу программы для каталога /usr и
# диапазона (мин.- мах.) 1000 1010.

find ./$3 -size +$1k -size -$2k -type f -printf "%p \t%s bytes\n"